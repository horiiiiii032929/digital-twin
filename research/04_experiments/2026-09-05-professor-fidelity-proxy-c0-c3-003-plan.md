# Professor fidelity proxy C0–C3 successor 003

## Decision question

Can the frozen synthetic 12-case C0–C3 proxy execute after correcting the one
provider-schema incompatibility that invalidated attempt 002?

## Single change

Translate provider-unsupported array/string assertions (`uniqueItems`, item
counts, and string lengths) out of the transmitted schema and enforce the same
contracts deterministically after parsing. Dataset, synthetic profile,
conditions, models, prompts, gates, seed, call limits, and USD 3 ceiling remain
fixed.

## Interpretation

A pass is `Go Deeper`: it is evidence that the synthetic profile has a measurable
effect under this proxy. It is not real-professor fidelity evidence and cannot
approve the profile. A valid hard-gate or advisory-quality failure is `Refine`.
Transport, identity, accounting, or harness corruption is `invalid-execution`.

All outcomes are recorded once. No reviewer search or tuning follows this run.
