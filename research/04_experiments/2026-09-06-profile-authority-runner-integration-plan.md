# V9 runner and usable application integration

Prospective draft, before shared source edits or V9 provider outputs.

Preserve V4–V8 exactly. Add V9 to the existing paired selector using the V8 compact response fixture, task question_specific_profile_authority, implementation question-specific-profile-grounded-v9, and instructional_profile_authority_enabled=True on top of compact plus V4 admission flags. The new arm retains Luna, low reasoning, 3,000 output tokens, identical public inputs, actual histories, and source-ID provenance. Do not enable V5–V7.

Repeat the same three development packets with unchanged seeds and previous gates. Add the prospective 12/12 appropriate-move requirement over explanation-first, compound-source, and mixed-no-evidence targets; it addresses known V8 failures without retroactively declaring the V8 engineering gate failed. Bounds remain 800 calls/USD20, 80/USD2, 120/USD3. All unfavorable outcomes remain registered. Confirmation remains closed pending the complete decision.

The simplest usable application hook is a separate ASGI factory adjacent to services/api/app/main.py. It loads the existing non-demo AppSettings, selects the explicit experimental profile, constructs the same Luna transport at the selector's 3,000 cap, calls the existing create_app with the exact selected flags and bounded provider concurrency, and verifies the actual generator ID. Existing middleware, authentication, persistence, source ingestion, publication, and autonomy services remain in use. Existing main.py/default release is unchanged. Proposed one-command shape: APP_EXPERIMENTAL_TUTORING_CANDIDATE=v9 uv run uvicorn services.api.app.experimental:create_experimental_app --factory --host 127.0.0.1 --port 8018, with existing staging TLS/origin/account configuration required rather than bypassed. The exact documented command must match a verified local setup; this draft is not yet runnable.

A versioned experimental profile must link accepted evidence and exact configuration, name remaining semantic limitations, and explicitly exclude whole-project completion qualification. Root owns that artifact after outcomes. The startup hook must reject missing/unrecognized candidate selections, incompatible configured runtime modes, model/cap drift, and actual ID mismatch; no automatic release/default change. Test the factory and authenticated API through the actual configuration before claiming usability.

Professor preview remains a separate gap. Current TeachingProfileService.preview hashes ten expected-behavior strings and profile content; no actual generated response or candidate/source binding is approved. A minimal substantive extension needs an isolated generated-preview artifact containing exact source/version hashes, draft profile hash, candidate/model/cap, actual inputs/responses, bounded usage, and response hash. Professor approval must bind that artifact, and source/profile/configuration drift must invalidate it. Do not create student dialogue records or learning-gap signals for previews. This is more than a labeling change; do not claim it implemented until actual draft-profile, source authorization, generation, approval, drift, and failure tests pass. A proposed implementation should reuse the generator/retrieval interfaces, not import evaluation scripts into product routes.

Pre-output ASGI clarification: the explicit experimental selector deliberately overrides the tutoring, planner, and evidence-admission modes to the evaluated governed graph, Luna policy planner, and scoped evidence gate. It rejects an independently configured external base generator to avoid a second provider path. Existing staging authentication, durable paths, HMAC secret, base profile and T1 qualification validation remain mandatory. Model/cap/reasoning checks traverse known client/transport/serializer wrappers as well as bare OpenAI transports, so the HTTPS recorded wrapper cannot hide an inconsistent actual provider cap. These choices are covered by real authenticated request and negative configuration tests.

Final pre-output review supersedes the preceding override proposal: the ASGI entrypoint rejects incompatible tutoring/planner/evidence modes instead of replacing them. It also rejects unknown leaf transport capabilities unless an explicit injected-test adapter declares matching model, output cap and reasoning. This preserves the prospective fail-closed startup requirement; the documented staging configuration states the three required modes.
