# Walkthrough coding repair plan

Run: walkthrough-codefix-004. Preserve all app decisions and evaluation design. Control: current dirty workspace, recorded source snapshots and frozen hashes under output/browser-qa/walkthrough-codefix-004. Candidate: decouple optional metadata reads from primary chat completion with guarded UI state and read-only retries; clarify synthetic previews. Prediction: stalled/failed reads cannot block chat; no extra tutoring requests or changed request payloads. Synthetic deferred/failure fixtures cover normal, failure, stale, navigation and duplicate-submit paths. Metrics: assertions, request counts/IDs and unchanged backend/profile/dataset hashes; no paid calls, no AWS operations. Run frontend tests/lint/build and existing API/accounting controls, then local browser lifecycle. Preserve failures and coverage limitations in a new result. No algorithm replacement, dataset split or scoring changes.
