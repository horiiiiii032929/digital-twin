# Meaningful-continuation packet v2: prospective setup correction

The first eight-context contract run failed before provider calls. Explanatory profiles in packet v1 contained one help-ladder entry, while the actual `TeachingProfileV1` requires at least two. The runner separately used duplicate course objectives; that adapter defect is corrected by its owner. The original zero-call failed run and v1 packet/seal remain unchanged.

Before any candidate output, v2 appends one explicit later-stage item to explanatory profiles: “On a later follow-up, address remaining confusion; a check question is optional.” This meets the product schema without making a same-turn check question mandatory. Sources, questions, gold meanings, numeric gates and all other profile values are preserved. Calibration anchors receive the same profile-only successor correction.

Both development and confirmation v2 profiles were constructed through the actual `new_teaching_profile` product function by the packet author. Confirmation contents remain unexposed to the candidate implementer, runner owner and primary agent; this authoring validation does not execute or tune on confirmation. The new seal records its bytes separately from v1.

Regression tests now construct every development profile with the real product model, reproduce the v1 one-stage rejection, verify unchanged gold/source/student questions, and check the v2 confirmation seal without parsing confirmation cases. This corrects an evaluation integration defect; it neither relaxes the product model nor changes quality labels after observing outputs. The parent must bind contract002/live execution to `meaningful-continuation-development-v2.json` and preserve the earlier failure.
