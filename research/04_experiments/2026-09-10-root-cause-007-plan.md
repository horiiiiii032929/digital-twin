# Root cause investigation 007

Question: for each remaining lifecycle-local-006 failure class, is the observed behavior caused by incorrect code/transport or the preserved model, assessment, publication or operational contract? Use the existing 80 paired synthetic turns and local database as read-only evidence. Do not restart paid providers or AWS. Preserve the slide compatibility plan, profile, prompts, thresholds, audit decisions, retry limits and schema.

Prediction: malformed binding outputs are correctly rejected; timeout plus unknown usage correctly closes the process budget; repeated teaching and NOT_ASSESSED are not sufficient evidence of missing history or display corruption. Inspect serialization, role routing, persisted evidence and UI mapping to test those alternatives.

A suspected additional coding defect is loss of the known provider failure code before final audit: the turn-80 budget rejection is flattened to provider_or_schema_failure in the persisted trace. Reproduce using deterministic clients; if confirmed, retain an allowlisted code only. Acceptance: identical answer, action, usage, citations, request payloads and call count; no arbitrary exception/secret content; existing final-audit diagnostic behavior unchanged. Record both failed baseline and retest. Do not classify improved diagnostics as recovery from the provider failure.

Use synthetic tests for the selected V19 route plus existing audit, provider budget, history, evidence, publication and frontend regressions. Document every finding's causal evidence and uncertainty. Any additional coding change requires a failing reproduction first. Record a new result and registry entry without overwriting prior results. This is application investigation, not a replacement semantic/learning evaluation.
