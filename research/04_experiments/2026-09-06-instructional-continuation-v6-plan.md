# Instructional continuation v6

Preserved development run `paired-pedagogy-development-001-live-001` produced29
v5 guarded failures despite zero provider failures. Read-only replay of all76
instructional proposals explains all29 against archived release chunks:12
terminal-question-mark checks,3 hint responses containing feedback/steps,3 ask
responses containing feedback/steps,10 full-proposed-answer hint guards, and1
nonexact hint quote. These are first failing checks; causes can overlap. Keep all
original results, source archives and semantic reviews.

Candidate v6 explicitly preserves v5 behavior as control/reproduction. It accepts
bounded, nonblank imperative elicitation; a trailing question mark is not a
pedagogical requirement. Typed feedback can precede a next prompt after an exact
current-message attempt. A cited mixed response uses the existing answer action,
while pure elicitation remains question; action alone never indicates full
solution disclosure. Existing explanation steps remain bounded and fully linked.

Typed partial guidance uses bounded text and checked aspect indexes, with exact
source bindings recorded honestly. It is model-proposed partial guidance, not
machine-proven withholding. A narrow literal full-answer-copy check may remain,
but semantic full-solution paraphrases can evade it; deliberate contract tests
must show this limitation and independent zero-critical-disclosure gates remain.
Do not call source bindings entailment. No judge or repair loop is added.

The unchanged quality rubric also found inadequate no-evidence boundaries.
An explicit overridable boundary renderer lets v6 name a missing detail using an
exact current-message focus and offer a supported part only when its assessed
approved bindings validate. No-evidence status remains. A typed third-party
private-request reason produces an explicit privacy refusal. This is a model
classification, not a keyword exception: tests include ordinary privacy-policy
discussion and own-information questions, plus actual third-party requests.

Before paid comparison run provider-envelope and actual-graph tests for
imperatives, supported feedback plus prompt, partial paraphrase, missing detail,
privacy, invalid focus/binding and retained defaults. Keep model Luna and3,000
output cap in both arms. Reuse the same development packet openly to test a
successor against v4; retain v5 unfavorable results. Confirmation stays closed.
Independent review covers every target and critical disclosure/unsupported
content regardless structural acceptance. The existing prospective runner's
800-call/USD20 bound remains; no hidden budget/cap changes after results.


## Pre-run design refinement

Remove the v5 full-answer quote-overlap rejection from typed v6 guidance rather
than make feedback an escape hatch. It cannot distinguish legitimate correction
after an attempt from premature disclosure, and does not detect paraphrased full
solutions. Full copied or paraphrased guidance can pass binding; an explicit
planted contract must demonstrate this and independent zero-critical-disclosure
review remains mandatory. Profile adherence is model-assessed. The legacy
`initial_elicitation` flag applies only without supplied profile context and must
not be described as enforcing approved Socratic initial withholding.


## Pre-run partial-response representation

For missing-evidence requests whose approved material supplies a useful known
next step, optional supported_response_aspect_indexes identify supported exact
spans for an explicit partial ANSWER with citations. The visible response and
unresolved_detail trace retain the missing current-message detail. Unsupported
indexes fail closed; no unknown value is fabricated. Empty indexes keep offer-only
behavior; initial Socratic withholding remains model-assessed and is a semantic
review requirement, not proven by a nonempty support index. Privacy remains an
explicit refusal. No response action is relabeled no-evidence while carrying
citations. One secondary focus-case mismatch found during punctuation replay
remains a strict-lineage rejection and is disclosed rather than silently normalized.
