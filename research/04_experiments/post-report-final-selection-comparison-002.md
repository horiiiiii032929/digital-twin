# Final selection comparison 002

Prospective paired development: V4 vs `v19-luna-luna-medium`, twelve frozen packets
`post-report-final-selection-v1-case-01` through `-12`. Each has two student turns
per arm (48 delivered outputs). Cases 01–05 are exposed regressions from earlier
five-context probes; 06–12 are new assistant-authored synthetic transfer cases
written before this candidate runs, not sealed independent human truth. These
cover necessity vs sufficiency, retry isolation, hypothetical withdrawal, absent
evidence, untrusted log instructions, missing versus zero and Socratic staging.
Real access/withdrawal enforcement and recovery use separate injected integration.

Same sources, profiles, student-turn schedule and seed7801, one history per arm.
Later histories include each arm's own delivered reply (end-to-end comparison,
not an identical-history component ablation). Context retrieval enabled for the
candidate as in prior probes; baseline unchanged. No other learning-state flags
are implied by this reactive-generation comparison. Models: Luna low planner and
draft; Luna medium audit/repair. Caps3000, maximum144 calls and US$12 reservation
per four-output packet; sequential reservations are refunded only with known cost.
No Sol calls. Cumulative budget US$30. Single pass over all twelve packets; no
selective regeneration of bad answers. All failures retained.

Use the calibrated version003 external Mini instrument, same prompt and source
payload for both arms, two independently ordered ratings per output. Before
accepting product ratings require all64 known control decisions valid and correct.
If calibration fails, ratings remain diagnostic and no model-based promotion.

Primary: fraction of answers both ratings judge acceptable, with failures,
quarantines and uncertain judgments retained in denominator; report grounding,
coverage, profile and citation defects separately. Candidate adoption requires
zero observed privacy/policy/citation boundary failures, no increase in grounding
defects, a strict paired improvement whose95% paired bootstrap interval excludes
zero, and separate passing integration checks. Otherwise Keep incumbent/Refine.
Bootstrap unit is paired two-turn case, not individual repeated ratings (10000
seeded resamples, seed20260908). Report exposed/new cases separately. Small sample
cannot certify95% population quality or real learning. Record all calls, latency,
output validity, timeouts, token cost, source hashes and rollback.
