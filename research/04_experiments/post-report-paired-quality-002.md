# Post-report paired quality development 002

Prospective plan, 2026-09-08, after reviewing run 001. Compare the existing
integrated `v14-luna-sol-medium` candidate with V4 on the same exposed
`post-report-course-shaped-quality-v1` packet. This deliberately reuses the
24 development contexts to check whether the existing conditional revision
stage addresses observed failures. It is not fresh confirmation. No prompt,
algorithm, selected profile or historical output changes for this run.

Question: does the existing revision stage improve useful, supported responses
and follow-up teaching behavior relative to the V4 control? Prediction: revision
may improve complete answers and conditional wording, but cannot recover course
evidence absent from its input. No superiority is presumed. A new V4 realization
is retained; cross-run differences may reflect provider variability.

Run ID `post-report-paired-quality-002-live-001`. Same four courses, three
scenarios each, two profiles, two turns and one restart: 48 histories, 96 turns.
Seed 7801, one repetition. Synthetic permissions and source/rubric from run 001
remain valid. Provider sampling itself is not seed-controlled. Exact existing
role configuration is recorded by the runner: Luna low planner/generation,
Sol medium conditional revision, 3000 output cap. No new model selection.

Maximum 480 calls, US$8 conservative reservation, within the user's US$30 total
budget. Prior known spend US$0.0600604, zero unknown-cost calls. No budget reset
or overwriting an existing run. Preserve source snapshot during execution.

Use the same prospective rubric as run 001 and inspect all 96 delivered outputs.
Report correctness/support, requested completeness, profile/stage and useful
next action separately from execution. Major defects include wrong opening
polarity and answers omitted after a genuine attempt. Track unsupported added
permissions/guarantees, absence of course evidence, schema failures and latency.
Hard gates: complete histories, unchanged source, no cross-course/private
release, zero critical unsupported claims. Semantic hard gate stays unresolved
where judgment is uncertain. The 95% diagnostic targets do not qualify the
system on this small, dependent, exposed sample. Record slices and paired
history differences without population accuracy claims.

Outcome Keep / Refine / Go Deeper / Drop must link all outputs and uncertainty.
Independent human review remains pending; no promotion based on assistant
review alone. Leave the current selected profile and fallback unchanged.
