# Post-report audited generation probe 004

Prospective 2026-09-08 plan. Compare unchanged V4 against V18 (Luna-low planner/draft, Sol-medium final quality audit and bounded repair; 3000 output tokens per role), with previous-student-query-v1 enabled only on V18. No other new learning/assessment selectors are enabled. This bundled comparison cannot isolate the audit from context retrieval. Preserve V4 and the selected release fallback.

Five exposed contexts, in this fixed order: Atlas withdrawal explanatory, Birch missing values Socratic, Birch recall Socratic, Elm group-size explanatory, Birch causality explanatory. Each context retains all three course source cards. Two turns per arm, a restart between turns, one repetition, seed 7801. Total ten histories and twenty delivered responses across five runs. These are deliberately selected known failures, not a representative accuracy estimate. Runtime receives public source/profile/student inputs only; required meanings remain reviewer-only.

Prediction: the bounded contract repair avoids malformed partial responses; the exact-final-response audit reduces unsupported assurances and missed requested calculations; context retrieval restores relevant evidence for continuations. Risks are excessive withholding, stage violations, extra latency and confidently incorrect audits. Review all outputs against the original source and prospective stage rubric, including refusals and unchanged controls. Hard gates: no candidate-owned critical content or scope error; no missing/unknown cost; source unchanged; no dropped histories; no repeatable duplicate/restart effect. A passed small development probe is insufficient for profile promotion. Classify acceptable/minor/major/critical/uncertain, source availability, request coverage, teaching stage, and failure origin. Report disagreements and withholding explicitly. Human review remains pending.

Execute separate runs post-report-audited-generation-004-case-01-live-001 through case-05-live-001 sequentially. Each reserves US$12 and at most 144 total calls. The runner allocates 72 calls/$6 per arm; V18 divides those into 24 calls/$2 per role. For two student turns, each with at most two generation invocations, worst-case candidate role requirements are planner 2, draft/protocol 8, final-audit/repair/re-audit 12 calls; the audit cost bound is 12*$0.16=$1.92, within its $2 partition. The V4 control needs at most six calls at $0.025. The runner's overall conservative preflight is 4 turns*11 calls*$0.16=$7.04, within $12. Do not weaken partitions or retry a failed call secretly. Run another context only after the previous run's known actual cost releases its unused reservation in the shared US$30 ledger. Unknown cost retains the full reservation and stops this series. Prior known spend is $0.1375708. Preserve preflight and other failures.

Archive input packet, this plan, exact invocation and source snapshot in every run. Inspect per-call model, role, token, cost and failure logs. Runtime latency is persistent-service/in-process latency, not HTTP or production throughput. No semantic population confidence interval from five selected contexts. The engineering probe is outside the final five-hour frozen acceptance window.
