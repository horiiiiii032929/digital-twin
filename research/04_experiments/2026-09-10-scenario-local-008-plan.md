# Scenario local 008: authorized paid follow-up

The user explicitly authorizes additional paid model calls on 10 September. This is a new isolated run; lifecycle-local-006 and its failed accounting guard remain archived and unchanged. Retain the latest slide/deployed decision configuration, existing 250-call/USD5 process caps, no automatic retries or cap-reset loops, and no AWS deployment. Use fresh synthetic identities and published controls; do not claim these pass fresh professor publication.

Prioritize browser gaps: pending-reply reload and interrupted response recovery, session expiry, duplicate submit, long/Unicode inputs, explicit historical dialogue chain, privacy/course boundaries, and repeated visits. Begin with up to eight live turns; expand only for an identified unresolved scenario and record why. Compare persisted request/response uniqueness and unchanged failed-output withholding. Paid generation is allowed; this does not authorize silently replacing algorithms or evaluation methods. Keep worker-dependent scenarios separately identified under the existing disabled configuration.

Before any new coding fix, reproduce it with a retained failing observation/regression. Verify targeted browser behavior and appropriate tests after the fix. Keep all original reports. Append results, coverage additions and summary links; do not turn API-only tests or seeded fixtures into browser lifecycle passes. Record calls, reported costs, unknown usage, exact settings and limitations.

After the initial eight turns completed, extend only for remaining explicit cases: over-limit input rejection, cross-course/unsupported source requests, and independent browser-session isolation if available. No budget reset. Two reproduced frontend candidates are interrupted-send restoration/manual idempotent recovery and protected-401 sign-in recovery. Tab-scoped storage is limited to the pending request (including its synthetic draft), account/conversation keyed, removed when completed; no automatic tutoring retry. Session failures are scoped to the authentication revision that issued the request. Backend decisions remain the control.
