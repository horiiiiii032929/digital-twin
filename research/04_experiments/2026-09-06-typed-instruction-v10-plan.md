# V10 typed composition and fresh-reference priority: prospective design

V9 completed all provider calls without transport/schema failures and achieved45/48 main contexts and16/16 primary, but failed the unchanged twelve-target teaching-move check at11/12. One otherwise useful partial response added an absence notice as an explanatory unit with no source IDs despite also correctly filling missing_details; local source validation rejected it. Two fresh ambiguous requests received no-evidence before the generator because retrieval returned no hits; identical ambiguous requests with hits reached the existing router and clarified. Preserve every V9 output and its decision.

V10 is one explicitly combined composition/routing candidate, not an isolated causal experiment. Keep compact output field names. A union has factual units (explanation/feedback, one-to-five source IDs) and elicitation units (zero-to-five IDs). The factual source requirement is visible in the actual provider JSON schema. Keep absence notices only in missing_details; add one generic shape example with placeholder source IDs, no course/benchmark facts. Source IDs still establish only approved-source association. V8/V9 schema, prompt, payload and renderer defaults remain unchanged; V10 has its own task/class/flag.

Reviewed and approved routing scope: only a fresh conversation with no persisted history may use the existing deterministic V3 unresolved-reference route before empty-evidence termination, and only while the graph has no higher-priority refusal/clarification intent. Current approved concept labels retain the existing whole-label admission behavior. Do not create a history resolver from text concatenation. Existing explicit clarification-option resolution happens before the graph and remains unchanged. A genuine unknown but explicit question remains no-evidence; not every failed retrieval is ambiguity. The new opt-in callback first consults a public wrapper around existing V3 non-clarification routes, preserving explicit integrity and course/source boundaries. Current V3 has no dedicated private-data classifier: mixed deictic/private requests safely clarify but are not classified as privacy refusals. Do not claim that this proves privacy classification. Negative combinations must be evaluated conservatively and any unresolved limitation recorded.

Tests before dispatch: actual Responses nested union serialization and parsing; missing source on factual unit rejected; empty source elicitation permitted; exact V9 task/schema/prompt/default behavior; actual same fresh ambiguous question with empty/nonempty retrieval; known explicit source/concept with empty hits; explicit unknown question; existing history and clarification resolution; deictic+graded/private/out-of-course/unpublished combinations. Independent design review precedes implementation.

Evaluation remains the same openly reused48 main+8 boundary+8 mixed-stage packets with all existing gates, including12/12 appropriate teaching move targets. No extra judge, no relaxed gate, no confirmation access, no silent release promotion. Record cost/latency/errors/per-case outcomes and combined-candidate scope. No paid calls before stable contracts and root execution decision.

Before any V10 dispatch, add one general initial-Socratic instruction: elicit the decisive condition/result instead of embedding it in the question. This addresses an observed uncertain question-leak pattern without domain-specific content or a changed disclosure gate. Its effectiveness remains independently measured.

The generic provider converter removes minItems. V10 alone restores the factual-source minimum from its local schema after conversion; historical tasks retain the old conversion exactly. The [official Structured Outputs guide](https://developers.openai.com/api/docs/guides/structured-outputs) lists array minItems and nested anyOf support; actual model acceptance is still checked by the finite boundary-first live instrument.
