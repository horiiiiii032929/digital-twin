# Local R1 governed V2.1 release qualification 005 plan

## Decision question

Does the exact release composition selected by immutable development evidence
operate correctly through the local HTTPS product boundary?

The composition is fixed before execution:

- `governed-autonomous-tutoring-graph-v2.1`;
- `guarded-policy-value-planner-v2` with exact `gpt-5.6-luna` planning;
- deterministic grounded generation;
- `dominance-scoped-ambiguity-safe-v3` evidence gating;
- profile `student-tutor-r1-local-candidate` version
  `v2.1-floor-004-h-e1`;
- T0 grounded-assistant rollback.

The normalized provenance binding is
`governed-full-autonomy-v2-1-release-binding-correction-001`. It composes the
immutable confirmation-024 autonomy result with product-evidence-gate
selection 004. It is not itself a release qualification.

## Fixed checks

Run the existing local R1 workflow through internal-CA HTTPS using synthetic
identities and open demonstration material:

1. Build immutable API, web, ingestion-worker, and outreach-worker images.
2. Validate startup rejects any profile, planner, evidence gate, or
   qualification-record mismatch.
3. Complete the 25-check administrator, professor, and student journey.
4. Restart the services and complete the six persistence checks.
5. Switch to T0 and complete the six rollback checks.
6. Restore governed V2.1 and complete the six restoration checks.
7. Record image digests, exact revision, configuration identifiers, artifact
   hashes, failures, and limitations.

## Decision rule

- `Keep`: all 43 checks pass, every exact binding holds, and no secret,
  private source, real student, or unrestricted model output is committed.
- `Refine`: the valid product composition fails a workflow, persistence,
  safety, grounding, or rollback check.
- `Invalid`: the environment, harness, or transport prevents an interpretable
  run. Correct only the demonstrated operational defect; do not change the
  selected method.

This qualification is operational integration evidence only. It cannot turn
selection 004 into an absolute grounding-quality pass. That selection's
corrected answerable factual success is 42.50% versus 26.00% for its control,
and both arms fail the absolute product-quality gates.
