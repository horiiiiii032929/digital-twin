# Eight-history final-candidate operational comparison

Prospective draft; no provider calls. Run only after the candidate quality decision and exact configuration freeze.

Question: does the accepted experimental instructional configuration preserve actual persistence, restart, consent enforcement, and source-bound autonomous follow-up under the same synthetic schedules as V4?

Exactly eight histories: versions V4 and final candidate, each crossed with reactive/autonomous conditions and personas fast-learner/low-receptivity. Use one declared Socratic profile, existing fresh operational source cards, seed6209, and thirty virtual days. Persona labels parameterize synthetic attendance, incorrect attempts, and response likelihood; they are not measured human traits. All eight selected specifications and candidate settings must appear explicitly in the run manifest. Override the inherited 24-history matrix in top-level execution metadata; never claim 24 executed. Distinct IDs include generator version, persona, condition, and seed.

Use the existing run_history implementation and its existing fixed attendance draws. Matched arms share seed/persona/condition, with generated histories allowed to diverge. Each history restarts on virtual day15; consent is disabled on day10 and restored on day20. Existing daily autonomous polling remains bounded at ten due actions. Fixed source cards, profile, model, 3,000-token cap, candidate flags, exact source snapshot, and orchestration script are archived before execution. Do not alter schedules after outcomes.

Execute at most two independent histories concurrently, balanced across versions. Use one cumulative recorded transport per version, with a total 5,000-call/USD125 ceiling (2,500 calls/USD62.50 per arm, conservative USD0.025 reservation per dispatch), and existing 600-call per-history ceilings. This covers at most 90 student submissions/history plus perception/generation/repair and bounded autonomous action dispatch without unbounded provider work; hitting any ceiling marks the run incomplete rather than dropping cases. Actual usage and cost are recorded separately from reservations. Provider calls have a finite 30-second timeout. No new model retry policy.

Hard operational gates: all eight complete thirty virtual days; all eight actual IDs match before/after restart; zero delivered outreach during each disabled interval; zero wrong learner/course/release lineage; all learner replies reference a real delivered message; no duplicate client-request persistence; persisted state and messages survive restart; all budget chains are observable with no unknown costs or missing failure records. Include all safe actions, provider/schema/local errors, and off-policy/unsupported outreach content in the report. Count consent changes separately from evaluated delivered messages and report zero-delivery slices as untested utility, not successful intervention.

Compare reactive versus autonomous and V4 versus candidate separately; no causal learning or human engagement inference. Review every delivered outreach and linked reply for actionable, course-grounded content and stage suitability. Report actual turns, deliveries, replies, guarded failures, latency, tokens/cost, and per-history denominators. Synthetic receptivity is not intervention effectiveness. These are thirty accelerated virtual days, not thirty days of uptime. Contract test with injected transport precedes any paid run; register all attempts and retain incomplete histories.

## Prospective generation-role variant amendment

Before any variant operating output, extend the same eight-history composition to explicit `v10-luna-low`, `v10-luna-medium` and `v10-sol-low` aliases, one selected alias versus V4 per run. The previous V9/V10 Luna-only plans and results remain separate and unchanged. The generator role varies; the planner remains Luna-low. Algorithm ID alone is insufficient: record and check the actual generator model and the reachable role-router configuration before and after restart. Fixed synthetic actor, cards, profile, 30-day schedule and consent/restart checks remain unchanged.

Use the shared recorded-generation-role helper with separate role ledgers and equal fixed partitions. Each alias comparison has a ceiling of 9,600 calls/USD 1,536 total, 4,800/USD 768 per arm; the candidate's two role partitions are each 2,400/USD 384 with USD 0.16 per-attempt reservation. Four histories per arm each retain the 600-call/USD 100 inner cap. Thus either role alone can cover all 2,400 possible arm calls without an artificial half-budget truncation; unspent role allocations are not pooled. These deliberately conservative ceilings are not expected spending. Historical V9/V10 ceilings remain 5,000/USD 125. The V4 transport stays Luna-low with its original per-call reservation, even when a variant run grants the larger outer ceiling.

Injected eight-history contract and actual role metadata/restart tests precede any separately authorized live operating comparison. A successful operating test does not replace the model variant's independent semantic qualification or establish student learning or intervention utility. Archive the shared role-helper source with the run.
