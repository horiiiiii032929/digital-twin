# Explicit authorized referent admission

Decision question: can a current question naming exactly one approved release
concept reach question-specific answerability despite a later demonstrative,
without relaxing integrity, source authorization or true ambiguity boundaries?
The prior six narrowed Cobalt follow-ups were blocked before any model call by
`explain that`, although the current sentence named Cobalt. Preserve that result.

Control: v3 bounded contract with incumbent context-free router. Candidate v4:
pass current-release approved domain labels through the actual student service;
when exactly one such label appears as a whole phrase in the current message,
defers a matched demonstrative-ambiguity heuristic to existing question-specific
answerability. This is admission of explicit context, not proof of resolved
meaning. No history-derived guessed label, arbitrary synonym, query-string
exception or policy disabling. Multiple named concepts, no named approved
concept and unknown concepts retain incumbent ambiguity behavior. Integrity,
cross-course/unavailable-source restrictions and approved evidence checks still
run. The model can still clarify an unresolved local referent.

Keep v3/default release unchanged. Expose a separate opt-in v4 flag requiring v3.
Before live calls test actual service propagation, authority binding, zero/multiple
names, whole-word matches, context-free baseline, missing evidence, cross-course
and integrity restrictions. The approval/release authority checks remain intact.

Finite named replay and fresh development: six prior failed narrowed turns
recreated as three repetitions per arm, plus explicit novel Amber/Violet named
referents, unresolved demonstratives, unknown names, multiple names, absent
numeric detail, cross-course, unavailable and graded completion negatives.
Use identical current stimuli and source/profile per pair, actual prior
conversation for the replay, 1,500 output tokens, 30-second timeout, no retries.
Bounds: at most 120 calls / USD 2.40 conservative reservation across both arms.
Record all current/previous turns, model messages, actions, validity, citations,
latency/cost/source hashes and per-case failure classes. Mechanical admission
must not be confused with semantic correctness; transparent assistant review
checks actual answers and remaining ambiguity. This consumed/fresh synthetic
mix is development, not held-out or human qualification.

## Follow-up contract audit (after preserved live run)

The live fixture uses unique labels. A subsequent code audit identifies a
conservative boundary gap: distinct approved concepts can share the same display
label, so deduplicating label strings must not establish unique concept identity.
After concurrent source freeze, preserve multiplicity and test that such a label
still clarifies. No new positive live claim follows from this regression.

The evaluator also reports one restart unconditionally; single-turn fresh cases
have none. Preserve the raw run, correct the durable restart total from26 to6
with per-history original values, then derive future counters from actual branch
execution and test single-turn versus multi-turn histories. This is bookkeeping,
not a change to past dialogue or model outcomes.
