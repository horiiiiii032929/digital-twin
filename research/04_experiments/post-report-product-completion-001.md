# Post-report product completion 001

User instruction: implement all identified improvements, with implementation time outside the five-hour evaluation allowance. Preserve the submitted report; disclose changed behavior as post-submission development. External-model spending remains bounded by the existing total US$30 ledger.

## Decision questions and predictions before implementation

1. Does using explicitly assessed, scoped observations instead of delivered-message counts improve the meaning and usefulness of planning inputs? Prediction: delivery alone cannot change evidence estimates; relevant correct/incorrect attempts change them in the expected direction. Missing, foreign, unassessed and future data must not count.
2. Can goal completion recognize recovery after incorrect attempts without allowing unrelated, duplicate or stale evidence to complete a goal? Prediction: two distinct recent correct attempts after the latest assessed contradiction, for every exact objective concept, permit bounded software completion. Partial/incorrect evidence resets the consecutive-success window. Preserve the original cumulative rule as a control. This is a software threshold, not validated mastery.
3. Do source selection, explicit request coverage and guarded response revision improve delivered content? Compare the current source-bound fallback and frozen candidate on the same full-course sources and multi-turn histories. Do not relax the critical-error gate or replace outputs with demo text.
4. Can the professor inspect actual configured responses and aggregate difficulties, revise and publish, while the same API/worker composition honors current authority and survives restart?

## Candidate boundaries

Implement learning changes behind explicit factory parameters and experimental profiles. Historical defaults remain available as controls until comparison and profile selection. Both reactive and proactive paths must use the same selected identity; runtime status and generated previews must identify the new composition. A new goal rule must see observations involved in the current atomic turn, while an aborted commit must not persist a completion.

Estimation arms: evidence count, decayed count, BKT and PFA. Planning arms: event rules, analytic-only selection, guarded model proposals, all with the same authorized input and action set. Compare input and policy independently before selected compositions. Model-free traces remain distinct from live external-model results.

## Evaluation inputs and gates

Version synthetic fixtures before the named comparison run, including normal, empty, sparse, partial, contradictory, repeated, foreign-student/course/release, missing-source, equal-time, out-of-order, expired and interrupted histories. Include missing and ambiguous objective mappings. Preserve and label exposed development cases; use separate newly frozen confirmation conditions when testing transfer. Exact sample grid is fixed in the execution manifest before calls.

Hard gates: zero unauthorized deliveries; zero unsupported goal completions against the prospective rule; no duplicate effect on replay; no delivery-only estimate increase; no silently missing/unknown provider costs; no candidate-owned critical semantic error for generation promotion. Existing factual/pedagogical thresholds remain unchanged. Uncertain semantic judgments are not successes. Assistant review is not independent human review.

Measure per-case behavior, classification, predictive Brier/calibration and simulator-specific estimate error where applicable, intervention usefulness under declared synthetic assumptions, latency, process memory, calls/tokens/cost, source hashes and configuration. Preserve every run, including preflight failures, Refine and invalid results, in the result registry and machine-readable records. Record a Keep/Refine/Go Deeper/Drop decision and a fallback before profile promotion.

## Sequence and evaluation allowance

Implement central learning/goal and answer improvements, professor and student workflows, operational boundaries, then synchronize evidence and presentation assets. Read-only diagnosis and engineering regression checks during development are not the final five-hour acceptance evaluation. At the frozen acceptance stage allocate 20 minutes evidence/configuration, 60 integrated workflows/failure, 80 semantic review, 45 learner/policy comparisons, 35 regression/accounting, 40 presentation/demo acceptance and 20 targeted reruns.

Human reviewer participation is not a completion requirement: the user confirmed that human real-answer review is unavailable and requested model-based review. Label the resulting evidence as model-based; it does not establish human learning effectiveness. Real-course permissions, external deployment credentials and long-term observations remain separate prerequisites only for work that requires them.

## Concrete implementation candidates (before comparative runs)

- `previous-student-query-v1`: for an explicit continuation after an answered exchange, supplement current-query hits with independently gated retrieval of the previous student question. Never query with tutor-authored prose. Do not cross a named concept switch or carry over a boundary/refusal. Control remains current-message-only retrieval. The exposed Birch causality case is a regression example, not fresh confirmation.
- `source-bound-literal-v1`: only exact approved text ranges can support configured literal target statements. Unsupported target text, negation, uncertainty, extra claims and paraphrases abstain. This intentionally narrow baseline must be compared for both precision and coverage; a high abstention rate is not a quality success.
- V17: V11 generation plus at most one structural/source-contract repair. Never silently convert partial to instruction or weaken source admission. Measure repair rate, useful recovery, added failure/cost and semantic regressions. Structural repair does not resolve semantic correctness.
- V18: V17 plus the existing version-2 final-response quality audit, now integrated into actual generation. Audit the exact rendered response using Sol-medium; a detected defect permits one issue-guided repair and one re-audit using the same bounded revision role. Uncertain, unchanged, rejected and failed repairs are withheld. At most five generation/audit calls (two draft/protocol calls plus three audit/repair calls), in addition to any planner calls. The audit remains fallible and is not an independent human judge or certification. Preserve V17, V11 and the release fallback as controls. Compare added withholding and latency alongside errors; fewer delivered answers alone is not improvement.

These candidates are unpromoted. Engineering regression runs are not new academic quality results. A named comparison must freeze the exact runtime combination and prospective case grid, preserve unfavorable outputs, and record any failed preflight before external calls.

For Q06, also implement an optional source-bound model assessor against the literal and historical lexical controls. It receives one unambiguous approved concept, approved text ranges and the current attempt; the student's assertion is never reference evidence. Require exact supplied source quotations for a non-abstaining outcome, validate outcome/reason consistency, retain unknown usage and abstain on provider/schema/source failures. Assess paraphrase, negation, partial explanations, appended unsupported claims, ambiguous/multiple concepts, missing material and boundary requests. Source quotation admission constrains provenance but cannot independently establish a model's semantic judgment. Do not promote or claim measured mastery before comparison.

## Follow-ups fixed before implementation, after probes 004/005 and assessment 001

V19 retains V18 and adds a narrow routing alternative for instructional withdrawal scenarios. The shared V3 control rejects any occurrence of “withdrawn”, including “What happens if an Atlas release is withdrawn while an answer is being generated?” even though all relevant authorized sources were retrieved. Candidate admission is limited to explicit hypothetical state-transition questions, with a named release/source/material state and no request to read, use, quote, disclose or retrieve unavailable content. It must preserve integrity, cross-course and actual source-access refusals, and still use the unchanged repository/current-release/evidence checks. Compare paired positive wording variants and adversarial compound requests before live use; do not call this general intent understanding. Retain V18/V3 as controls.

Source assessment V2 retains V1 as a selectable control. Both Luna and Sol incorrectly assigned a judgment to an unsupported target in assessment001. Require every target clause to be literally present within its approved canonical ranges before semantic attempt assessment. This is intentionally restrictive: paraphrased professor criteria must be revised or remain unassessed; student paraphrases can still be assessed. Strengthen the instruction so incomplete coverage of a supported target is partial, while an added source-unsupported assertion makes the whole attempt not-assessed unless the source explicitly contradicts it. Preserve the unknown-claim failure and the V1 results; evaluate V2 on the same 32 cases as exposed development. The target check is not a semantic truth checker or proof of learning.

## User-directed review and spending update, 2026-09-08

Preserve completed Sol results, but do not schedule additional Sol calls under this plan. Prefer the already supported GPT-5.4 nano and GPT-5.4 mini snapshots for added review. Larger sample volume is permitted within the unchanged total US$30 budget; it does not authorize unlimited calls. Freeze per-run limits and check the shared ledger before execution. Existing V18/V19 Sol configurations remain unpromoted experimental controls; do not silently substitute a cheaper model in their recorded identities or imply that a cheaper replacement has already passed evaluation.

Use model-based review as the requested formal semantic evaluation method. Hide generation model and candidate labels, provide the same source evidence and rubric for each arm, and retain abstentions and withheld outputs. Calibrate judges with predeclared controls; record disagreement, repeat consistency, and failures rather than treating consensus as ground truth. The proposed cheap-review model pair has not yet completed this calibration or the new review run. A versioned plan and reproducible instrument must precede paid execution.

## Bounded integration follow-up 002

Freeze `post-report-product-integration-002-contract-001` with V19, assessed-count, analytic-only planning, source-bound model assessment V2, goal recovery and continuation retrieval. Run the reusable mixed-source harness with `--post-report`, injected providers only; all requests, identity checks, review, withdrawal and clean restore must pass. Require the real final-audit task to be dispatched. This is not semantic quality or production qualification. Separately, the selected-worker regression exercises persisted opportunities, real SQLite reopening, lease expiry, replay and a crash between materialization and commit, for bounded wording success and typed provider timeout; all four combinations must preserve one delivery. These tests retain the existing proactive bounded-wording architecture and do not represent V19 reactive generation as proactive free generation.
