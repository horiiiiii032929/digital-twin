# Global support assessment before keep or repair

## Decision and prediction

The fixed V14-medium necessity diagnostic failed7/8 with one critical Topaz
converse assertion. The revision explicitly kept that assertion as supported.
Its prompt places implication/quantifier/entity guidance under instructions that
apply only after deciding a repair is needed. This is a plausible instruction
scope defect, not a proven sole cause. A budget/effort increase already passed
component controls but did not remove the integrated failure.

Compare unchanged V14-medium with a separately named V15 assessment prompt.
Apply factual-support checks before choosing either KEEP or REPAIR: evaluate
claims including question presuppositions against their own source and entity;
consider whether the source could hold while the asserted consequence fails;
preserve implication direction, quantifiers, exception and event/eligibility
boundaries. Explicit if/otherwise procedures still require direct application.
No benchmark names, values or answers enter the prompt. Keep schema, models,
medium reasoning, output cap, one revision, original-draft retention and policy
boundaries fixed. This remains a fallible model check, not a logical proof.

## Staged implementation and data

First implement only a separately callable revision helper and shared injectable
instruction interface. Preserve the V14 default prompt byte-for-byte. Do not
wire V15 into product orchestration before component evidence supports it.
Run existing112 revision controls (including adequate and flawed drafts) twice
and retain every result. Require both trials independently to satisfy the existing
40/40 old-adequate, at least30/32 old-flawed,8/8 disclosure,16/16 fresh-adequate,
16/16 fresh-flawed and zero-critical gates. All224 proposed component calls are
bounded atUSD35.84 total,112/USD17.92 per trial. Schedule repetitions are not new
independent examples. Retain V14-medium's two historical trials as a nonconcurrent
control and disclose reuse/exposure. All decisions require inspecting real output;
schema compliance is not semantic accuracy.

If both component trials pass, integrate V15 behind an explicit experimental
alias, run all regression checks and repeat the original eight necessity cases
plus corrected main, short, profile and boundary/mixed suites before opening any
sealed input. Add a prospective unambiguous procedure/constraint contrast packet
before integrated dispatch to detect indiscriminate hedging; preregister its
source/gold and exact run budget before calls. Do not rescue a failed trial with
an output-selected retry or relax thresholds. Record Refine/Drop on failure.
No selected release or report claim changes until evidence is reviewed.
