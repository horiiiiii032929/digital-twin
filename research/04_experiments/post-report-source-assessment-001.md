# Post-report source-bound assessment 001

Prospective plan, 2026-09-08, before any model assessment calls. Compare source-bound-literal-v1 with source-bound-model-assessment-v1 using Luna-low and Sol-medium separately. The same 32 synthetic cases cover four concepts (literal, paraphrase, partial, contradicted, unsupported appended claim, pure question) and eight scope/source/adversarial boundaries. Targets and sources are explicitly frozen in post-report-source-assessment-v1.json. Gold labels are assistant authored and not independently validated; never send expected labels, categories, mutations or case IDs to the model. The released synthetic component fixtures carry only approved target/source content; mutations are applied to deep-copied inputs for boundary probes, not published to users.

Prediction: the literal baseline abstains on paraphrases and negation; the model increases useful coverage without promoting unsupported or contradictory claims to correct. Measure exact four-class accuracy, macro F1, full confusion matrix, per-kind/class slices, non-abstaining coverage and conditional accuracy, correct-label precision, false-correct rate and Wilson intervals for binary rates. Abstention on a required assessable case is not counted as correct. No answer-learning benefit is inferred. Hard gates: zero false-correct claims, zero source/scope boundary assessments, exact provider/schema/usage, source unchanged and all 32 cases accounted for. Gate success on a small synthetic sample still requires further confirmation before default selection. Failure classifications: data/label ambiguity, source, parsing, model, policy, integration and operations.

Use actual SourceBoundModelAssessor/assess_source_bound_attempt interfaces, actual bounded OpenAI Responses serialization and versioned course domain/ranges. Provider calls are sequential, one repetition, no model seed guarantee. Luna output cap 3000 low; Sol cap 3000 medium, explicitly experimental. At most one call per case, shared recorded bound 32 calls and US$6 per model (32*$0.16=$5.12 maximum reserved allowance). Run model arms sequentially and settle known actual cost in the user's US$30 ledger; stop on unknown cost, retain reservation, never silently retry. No private course data or external messages. All returned outcomes/reasons/quotes/usage are preserved with source and config hashes. Component runtime latency and memory measured; not network service throughput. Preserve partial/error runs and Keep/Refine/Go Deeper/Drop decision. Runs are post-report-source-assessment-001-luna-live-001 and post-report-source-assessment-001-sol-live-001. No automatic profile promotion. This is development work before the five-hour final acceptance window.
