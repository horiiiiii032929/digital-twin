# Generation model stability in instructional dialogue

Prospective follow-up after the entity/implication diagnostic's three-way28/28
ceiling result, before any outputs in this follow-up. That experiment remains
Refine with no positive gain; its selection rule is not relaxed or relabelled.
The new decision question is whether actual instructional histories and approved
profile handling reveal a useful generation-model difference that the short,
explanation-only logic diagnostic could not measure.

## Fixed methods and exposed data

Keep V10 code, prompt, schema,3000-token cap, source association and policy fixed.
Compare the same explicit Luna-low, Luna-medium and Sol-low generation aliases;
planning remains Luna-low. Use only exposed development data: the existing48
meaningful-continuation contexts and24-case approved-profile responsiveness packet.
No new confirmation, private course material or new prompt enters this stage.

Run two complete trials for each alias on each packet. Main paired runs use
schedule seeds7801 and7802 and retain their separately executed V4 controls.
For the scientific generator comparison, pair candidate outcomes across aliases
by original context ID and trial, not by the unrelated V4 outcome. All three
aliases receive identical source/profile/student inputs; their generated
histories can differ. The profile packet has no model-seed control: label its
two full independent provider executions as trials rather than seeded replicas.
Do not imply that scheduling seeds make model outputs deterministic.

The main design has48 unique contexts,96 target observations per alias, and
32 primary-target observations per alias. It does not have96 independent source
families or students. The profile design has24 unique conditions/cases and48
observations per alias, including12 context-on style opportunities. Preserve
all six main runs and six profile runs, including unfavorable outcomes.

## Prospective decision rules

Review every delivered turn using the unchanged meaning-based rubric and the
profile-specific checksum, allowance and prefix anchors. Retain uncertainties
as unqualified and earlier critical events as invalidating their later target.
A candidate eligible for further selection must satisfy all of:

- at least92/96 definite useful main targets (the smallest integer meeting95%);
- each main trial meets the existing39/48 and13/16 coverage floors, and each has
  all12 appropriate explanatory moves;
- zero verified critical errors across both main histories and both complete
  profile runs;
- all12 context-on style responses are useful, all six matched profile contrasts
  are substantively appropriate, and every dispatched profile binding matches;
- no reduction in main useful-target count versus Luna-low, and at least one
  net definite win over Luna-low in main or context-on profile observations.

These are stricter, newly prospective dialogue-development selection rules;
they do not retroactively change the failed short diagnostic or the project's
representative confirmation requirement. Report each trial, slice and paired
win/loss/tie/uncertain count. Do not force a win through ambiguous wording.
If multiple alternatives qualify, prefer more main useful targets, then fewer
local guards, then lower observed cost; latency is descriptive under co-hosting.
If none qualifies, retain Refine and investigate method-level failure causes.
Do not open the new confirmation merely because an alternative is more expensive.

A selected alternative must also pass the unchanged boundary and mixed-stage
sidecars before the sealed fresh48-context confirmation. Real-course and
same-configuration operation follow only if that confirmation is accepted.
Human instructor fidelity and learning outcomes remain outside this experiment.

## Execution and evidence

Reuse the existing paired and responsiveness runners without executable changes.
Use each main run's explicit alias bounds800attempts/USD128 and each profile
run's100attempts/USD20. These are conservative ceilings, not expected spending.
At most two runs may overlap; record co-resident full-check load and do not use
these timings as capacity evidence. No output-selected retries, prompt changes,
cap increases, dropped cases or early favorable stopping are allowed. Complete
all planned observations before comparison. Archive this plan explicitly with
input provenance; never read sibling sealed packets or their shared builder.
Register each named run and one aggregate decision linking every trial, revision,
source/input hash, role configuration, failure, cost and review. The previously
failed V10 confirmation and profile evidence remains visible.

For these named development comparisons only, this prospectively documented
follow-up replaces the earlier execution condition that profile runs require a
winner of the short diagnostic. It does not declare such a winner or permit
confirmation. Freeze an aggregate run-group manifest and this plan's byte hash
before dispatch; profile runs without a provenance CLI option link that immutable
manifest rather than requiring a harness change.

Independent assistant review before dispatch confirmed denominators and requested
these clarifications: the zero-critical requirement applies to each candidate
alias's own histories/profile outputs; separately executed V4 critical events
remain visible but do not disqualify an otherwise adequate candidate. An uncertain
paired judgment counts as neither a win nor a loss. One net win is an engineering
selection rule, not statistical superiority. Main observations share48 unique
contexts and four source clusters across two stochastic trials; do not compute
an independent96-sample confidence interval or treat repeats as new learners.
