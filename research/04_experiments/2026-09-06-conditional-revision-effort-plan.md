# Revision reasoning effort comparison

## Decision question

V14 Sol-low selected every defective control for repair but damaged one adequate
new-concept question and introduced an unsupported exclusive access condition.
A third response omitted the required initial comparison cue. The112-call result
remains Refine (109/112 useful, two criticals); its source snapshot and root-only
post-output review are retained. Do not reopen confirmation or lower those gates.

Before adding another model stage or subject-specific logic, test whether greater
reasoning effort in the existing Sol revision call improves these semantic
judgments. The candidate holds the V14 conditional prompt, schema, actual public
inputs, Luna-low draft/planner configuration and cap3000 fixed, changing only
revision effort from low to medium. The historical low run is a disclosed,
nonconcurrent reference; a stochastic repeat is not a controlled causal proof.
The prediction is fewer unjustified teaching-move changes and scope additions.
Greater reasoning may instead increase latency or exhaust the same output cap.

## Implementation and preregistered evaluation

Expose a separate v14-luna-sol-medium runtime alias and direct-component choice
v14-medium. Retain the original V14 alias and every release default. Record the
exact per-role effort, final provider on keep/repair, known usage and failure
stages. Model identity and source/policy checks remain unchanged; no prompt edit,
extra request or retry is introduced. Compare alias/routing/serializer and
persistent-runtime tests before the live component runs.

Run the same112 frozen controls twice under medium, in the frozen packet order.
Each trial has112 calls/USD17.92, concurrency1, cap3000, and one conditional call
per authored draft. Total224 calls/USD35.84; retain every trial and any incomplete
or unknown-cost outcome. Do not replace unfavourable outputs. These are56
repeated scenario pairs, not224 independent contexts. Use an isolated source
snapshot so unrelated preview fixes cannot change the trial.

Each trial separately must preserve all40 prior adequate and16 fresh adequate
drafts, repair at least30/32 old defects, all8 previous disclosure defects and
all16 fresh defects, with zero critical or uncertain-as-success judgments,
complete known usage and unchanged source/model configuration. Score content,
not model verdicts. Report all112 paired outcomes against the low reference,
false-keep/unnecessary-repair counts, family slices, tokens, cost and latency.
The only available post-output reviewer is the root coding assistant; additional
independent review and human validation remain unavailable, not invented.

Only if both trials pass may the explicit medium alias enter the existing
integrated Stage B with unchanged quality gates. Further source-course, full
quality-matrix,30-virtual-day,API/browser and capacity tests remain required;
passing these component controls does not complete the project or promote a
release. Failure yields Refine and investigation of the unhandled semantic
boundary, not an automatic fourth model stage or threshold reduction.
