# V10 typed compact response comparison

Prospective plan, before V10 implementation results or external outputs.

Decision question: can a separately versioned compact response contract eliminate the observed empty-source factual-unit rejection while preserving existing clarification, teaching-profile, and disclosure behavior? V9 remains reproducible and retains its main result of 45/48 useful contexts with the failed 11/12 appropriate-explanation gate.

The V10 candidate has a separate model task and schema. Factual explanation/feedback units require at least one source ID; elicitation units retain their own source requirements. This is provenance validation, not a semantic correctness oracle. A separate initial-turn admission correction applies only when there are zero prior messages, evidence is pending, and the existing V3 router already requires clarification. It does not reinterpret established conversation history. Exact implementation ID, flag configuration, prompt/schema hashes, and model settings must appear in each run manifest.

Retain the unchanged V4 control, Luna model, low reasoning, 3,000 output tokens, seed7801, fixed input packets, and actual generated histories. Earlier V5–V9 options, outputs, scores, and operational contracts remain unchanged. Add only an explicit V10 selector; do not use a mutable final/latest alias.

## Qualification and order

Run and register injected main representative, boundary, and mixed-stage contracts first. These validate wiring and preservation of histories, not acceptance by the external provider. After source and independent code-review freeze, run the complete eight-context boundary sidecar first (16 paired turns; 80 calls/USD2 cap). Inspect actual provider transport/schema failures, unknown-cost calls, completion, and source/input hashes before sending the main packet. If the new provider contract is rejected, retain the named outcome and stop further paid dispatch; do not call the fixture a successful provider qualification.

If operational qualification passes, run the unchanged main development packet (48 contexts, 168 paired turns, 800 calls/USD20) and then mixed-stage packet (eight contexts, 24 paired turns, 120 calls/USD3). All runs retain every failure and no output-selected retry. Sidecar semantic failures remain adverse evidence and do not disappear because transport succeeds.

## Decision rules and scope

All prior main and sidecar gates remain unchanged, including the prospectively added 12/12 appropriate explanatory moves across explanation-first, compound-sources, and mixed-no-evidence targets. Inspect all preceding turns for disclosure or false feedback, including uncertain outputs. Actions and valid source IDs do not determine semantic passing scores. These are openly reused development packets reviewed by assistants, not human fidelity or learning evidence and not the broad 95%/98% completion contract.

Confirmation, private real-course transfer, eight-history paid operational comparison, and authenticated browser/load evaluation remain closed until the complete candidate decision authorizes them. Their separate prospective budgets and source snapshots remain required. Existing candidate startup and operational selectors may expose V10 explicitly, but this does not select a release or imply qualification.
