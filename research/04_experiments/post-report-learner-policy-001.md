# Post-report learner and timing comparison 001

Prospective plan, 2026-09-08, before this simulation is run. Compare the exact new runtime defaults for evidence count, seven-day decayed count, BKT and PFA, without fitting them on this run. Use the existing seeded learner simulator and timing-policy interface as component experiments. For each estimator compare constant, conditional, value and never policies. Add count+oracle as an unattainable reference that is explicitly allowed to inspect hidden need. These timing policies are existing component controls, not a claim that the new actual-runtime analytic-only planner is identical to the simulator's value policy.

Grid: 30 virtual days, eight concepts, all original six and successor six personas, both BKT-like and logistic-like families, seeds 7100 through 7119 (20 seeds). Seventeen conditions, 480 histories each, 8,160 histories total. No external calls, no real learner data. No new tuning or post-result exclusion. These fresh seeds/conditions are synthetic confirmation of component behavior, not independent educational validation. All random streams and simulator assumptions remain unchanged; different intervention schedules consume randomness differently, so matched seeds reduce baseline variability but do not force identical observation sequences across policies.

Prediction: decay is more responsive to old evidence, but may worsen sparse-data uncertainty and useful intervention timing. BKT/PFA need not win without course calibration. Measure raw score versus simulator hidden-state MSE, raw probability next-outcome Brier/log loss/ECE, sends, waste, follow-up, no-action days, eligibility violations and final hidden state. The probability field differs in interpretation: BKT estimates latent mastery, while count/PFA estimate observed-success propensity. Therefore raw-score metrics evaluate how the shared planner score behaves; do not present raw Brier as a calibrated, semantically equivalent forecast comparison or latent MSE as validated mastery. Also compute BKT observation-space Brier separately using guess+(1-guess-slip)*posterior in an identical open-loop observation stream, compared with count/decay/PFA predictions. No policy preference may be decided from a mismatched target metric alone.

Primary policy checks: zero eligibility violations and no increased mean wasted interventions versus count+conditional without a compensating supported utility improvement. Summarize per family/persona, not just pooled means. Paired bootstrap seed 20260908, 1,000 resamples over matched histories; report synthetic intervals and avoid causal real-learner language. Estimator comparison primary metric is observation-space Brier on the same seeded self-directed stream, with no policy intervention. Secondary raw hidden-state errors are descriptive. Preserve all candidate results, configurations, seed grid, source hashes, per-history results, runtime, memory and zero external cost. Keep current selected control unless stronger applicable evidence justifies an explicitly recorded profile. New runtime still requires its own integrated lifecycle and failure/recovery tests; successful abstract simulation does not establish application correctness or long-term learning benefit.
