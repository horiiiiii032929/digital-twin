# Compact profile authority V9: prospective development comparison

V8 passed its three declared narrow gates (39/48 main contexts, 16/16 primary; 8/8 boundary; 8/8 mixed-stage), yet six of twelve explanation-first rule/compound/mixed targets received inappropriate questioning or withholding. Preserve V8 as the best working candidate and retain all outputs. In the actual requests all twelve targets received `diagnose_understanding`, help level zero and an observed-attempt false flag alongside an approved explain-first profile. This establishes conflicting context, not a causal explanation: six targets succeeded with the same fields.

The decision question is whether removing advisory planner fields when an approved teaching profile is present improves adherence without weakening safety or meaningful continuation. Prediction: fewer initial explanation-first quizzes and false absence claims. V9 retains V8's compact schema, source association, renderer and admission policy. It changes only (1) omission of pedagogical_intent/help_level/application_observed_attempt from model payload when approved profile context is non-null, (2) explicit single-instruction profile priority and distinction between pedagogical withholding and unavailable evidence, and (3) a neutral missing-information heading that can introduce either phrases or full sentences. Actual learner history and current message remain present. A null profile retains advisory payload fields. The profile cannot override evidence, consent, privacy, assessment or allowed-action boundaries.

V8's prompt, payload and rendering remain behavior-identical through default-preserving hooks; V9 has a distinct class, task and explicit flag. No new schema fields, semantic judge, repair loop or source granularity change. All source association remains structural only and semantics unverified.

Evaluate the same openly reused 48 main, 8 boundary and 8 mixed-stage contexts, unchanged rubric and prior gates, exact Luna identity and 3000 token cap. Add prospectively a component selection check: all twelve main explanation-first rule/compound/mixed targets must choose an appropriate teaching move. This is a twelve-case refinement target, not a population success-rate claim and not a redefinition of V8's earlier gate. No criterion is weakened. Keep unopened confirmation closed until all selection criteria permit dispatch. Preserve all model failures and outcomes, measured latency/cost and source/input hashes.

Contract tests must verify exact V8 prompt/payload preservation; V9 removes only the three advisory fields with approved profile, preserving question/history/profile/evidence; null profile fallback; distinct actual factory/restart/task registration; neutral heading; all prior compact boundary/source-association tests. Injected tests establish wiring, not that the model follows profiles. Independent review precedes paid source freeze.
