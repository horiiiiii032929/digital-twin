# V10 approved-profile responsiveness

Prospective, before candidate outputs. Reuse the openly exposed responsiveness
packet: two synthetic profiles, context off/on, three style questions and three
boundaries, yielding24 isolated actual-runtime cases. This is a narrow development
ablation, not a fresh confirmation or an actual-instructor fidelity study.

The question is whether the final instructional composition expresses different,
appropriate teaching moves when approved preferences are supplied. Hold the
V10 generator, Luna model, low reasoning,3000 output cap, question, evidence and
runtime constant within each pair. Explicitly switch approved-context handling
off/on. This intervention includes V10's conditional removal of advisory planner
fields when approved context is present; it is not profile-text-only causality.
Retain actual input payloads to verify this distinction.

An independent assistant reviewed the packet and this protocol before execution.
For the explanatory profile, require a specific supported explanation or
correction. For the Socratic profile, require diagnostic/contrast-first help
without the complete procedure; its misconception policy explicitly asks for a
contrast before correction. The explanatory structure's example/check sequence
does not unambiguously demand every element in one turn: report those elements
separately, without inventing that temporal requirement. Its misconception
policy does explicitly request an application question after correction.

Review all24 actual responses under the meaningful-continuation axes. Fixed
component criteria:6/6 context-on style responses useful,3/3 matched questions
show the appropriate substantive teaching-move difference, zero verified
factual/integrity/authority violations across24, and100% correct profile binding
on dispatched generation requests. A correct checksum explanation must not claim
collision-free certainty; escrow must preserve local allowance and total rights;
prefix compression must retain the suffix and reconstruct prefix plus suffix.
Context-off responses need not be bad. Different words or intent labels alone
are insufficient; old intent metrics remain descriptive. Missing/failed outputs
stay in their denominators. No generation request means binding is untested for
that case, not a propagation pass. Counterfactual results do not establish
authentic professor imitation, student learning, withdrawal or hash-mismatch
coverage; those remain separate evidence.

Use two concurrent cases, one trial, at most100 attempts/USD3, conservative
USD0.025 reservation per call and30-second timeout. Worst-case72 attempts cost
USD1.80 in reservations. An injected contract precedes live execution; run only
after the V10 development decision. Preserve all failures, raw payloads, revision
and dirty state, complete source snapshot and final hashes. Register every named
run. Keep old runner defaults and historical outcomes unchanged; no selected
release promotion follows from this small ablation.

## Prospective generator-variant extension

Following the failed V10 run, evaluate only a variant accepted by the separate
[generator comparison](2026-09-06-v10-generation-model-comparison-plan.md).
Explicit aliases `v10-luna-low`, `v10-luna-medium` and `v10-sol-low` retain the
V10 algorithm and approved-context intervention; the planner remains Luna-low.
The selected alias fixes the generator model and reasoning before dispatch.
This is still exposed development data, not a new confirmation.

For these role-separated aliases, retain all24 cases and the same semantic and
binding gates above. Use100 permitted attempts and USD16..20 total, with a
USD0.16 conservative per-role reservation and one shared outer call/cost budget.
Each role receives50 attempts and half the cost allowance. This covers the
conservative24 generation and48 planning attempts for this24-case reactive
packet; budgets cannot silently borrow from the other role.
The higher envelope is required for Sol pricing and does not change the original
Luna V10 run's bounds. Record planner and generation requests separately with
matching serializers, aggregate all attempts and usage, and inspect actual
generation payloads for approved-profile/advisory binding. An injected contract
must precede actual calls. No profile, question, source or rubric changes are
permitted after outputs; no improvement is inferred from intended model identity.
