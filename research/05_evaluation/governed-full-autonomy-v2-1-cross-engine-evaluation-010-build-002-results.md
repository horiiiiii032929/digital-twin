# Cross-engine evaluation 010 finite build result

Decision: **Go Deeper to the single bounded paid execution.**

The complete finite runner is implemented and provider-unauthorized. It holds
the factual method, autonomy graph, policy, prompts, clocks, cases, and scorer
constant while comparing E0-E5. Every engine receives the same 500 candidate
and 100 rollback-control factual cases plus the same 820 actual-product
autonomy cases. Only the top two eligible engines advance to the fresh 1,000
confirmation, and only a passing winner advances to the known 10,000+1,000
regression.

The new sealed confirmation contains 200 source clusters, 1,000 unique public
questions, 800 answerable and 200 boundary cases, 600 canonical evidence atoms,
and 963 exactly matchable required source ranges. It is byte-stable,
source-range-disjoint from prior development, public-only, private-data-free,
and was constructed with zero provider calls.

The program has one USD 50 emergency stop, exact direct-provider identities,
atomic per-arm and program ledgers, interruption-safe resume, one-way stage
gates, and no quality rerun on an opened sealed set. Supplementary professor,
usability, and learning outputs are labelled LLM/agent proxy evidence and cannot
be represented as real-human proof.

No paid execution or product-selection claim is part of this build result.
