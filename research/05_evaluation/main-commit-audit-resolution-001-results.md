# Main-commit audit resolution 001

Date: 2026-09-02  
Decision: **Refine / no governed V2.1 release selection**

## Outcome

All 24 published findings in `main-commit-audit-8786375-2bdfea9-001` and nine
additional reproduced product findings now have an explicit terminal
disposition. Twenty-eight are fixed prospectively in code, configuration,
tests, or product behavior. Five evaluation-integrity findings are closed by
invalidating or retiring the affected evidence rather than rewriting history.

The important correction is that confirmation 013, its post-hoc reference
correction, the derived conversation-orchestration selection, and the governed
local qualification cannot support a current V2.1 release claim. The response
outputs and every favorable and unfavorable historical record remain intact.
The active local configuration is the earlier bounded T1-v1 profile with
deterministic T0 rollback.

## Corrective evidence

- Runtime correction revision: `12658309356a82883232ff3b2be15b6e8514a418`.
- Python: 1,623 passed.
- Frontend: 50 passed; lint and production build passed.
- Changed Python files: Ruff passed.
- Provider calls and cost: zero.

The fixes cover the governed boundary router, proactive evidence contract,
clarification persistence, provider-call accounting, planner observability,
fallback filtering, generator/verifier compatibility, crash-safe delivery,
paused-work starvation, restart integrity, clock consistency, learning-gap
review authority, and professor-console race/N+1 behavior. Configuration and
test coverage now bind the exact gate, planner, profile, and safe Compose
defaults.

## Evaluation-integrity disposition

The post-hoc change of 30 gold actions is retained as historical analysis but
not accepted as prospective release evidence. Its false code revision, missing
012 correction record, and profile mismatch are not repaired in place. The
dependent V2.1 selection is revoked. Successor comparisons must use fresh
evidence, typed/hash-verified manifests, and one shared black-box evaluation
contract.

## Next decision

The repaired runtime is a fair common baseline, not a selected winner. The next
checkpoint compares deterministic workflow A, governed single-planner B,
hierarchical model-based planner C, and verifier-only C+V under the same engine
first. The best two architectures then receive the same economical model
allocation comparison. No historical 011–013 result is used to select the
winner.

This result establishes neither real-professor fidelity, real-student
usability, nor real learning improvement.
