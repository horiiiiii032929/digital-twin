# Medium-effort conditional revision preflight

The root coding assistant reviewed the explicit medium alias and direct runner
change before output. Only revision effort changes: Luna planning/drafting remain
low, Sol remains the revision model, prompt/schema/input/cap remain unchanged.
A serializer assertion confirms the actual outgoing medium setting. Original
low aliases remain selectable. The helper and provider-file hashes match the
low component snapshot; current G2 files are outside this isolated experiment.

Main-tree controls and generated-preview tests passed33 cases before the added
serializer assertion; that assertion then passed separately. In the isolated
component snapshot12 tests passed, with3 integration tests explicitly excluded
because its whole-application selector is intentionally absent. The earlier
112-case injected wrapper contract remains applicable to the unchanged helper;
this preflight does not claim a new semantic calibration or human review.

The root-only review and exact overlays are recorded in
`reports/generated/conditional-revision-controls-v1/medium-isolated-source-manifest.json`.
The frozen112 packet remains SHA158b135f656fd116b4801f49374fbf85eb9bec326e68a0a63ff488888e599b0b.
Two complete trials each have112 calls/USD17.92 under the prospective
[effort plan](../04_experiments/2026-09-06-conditional-revision-effort-plan.md).
Do not dispatch a second trial after unknown usage stops the first. Neither
trial may replace an unfavourable result or open Stage B alone.
