# Evaluation result: post-report-product-integration-001-contract-001

## Run identity

- Component: post-report product integration, synthetic contract only.
- Status: completed; 2026-09-08, Codex primary review.
- Code: `9b16c15b8c1463b4f2ed1bbdbe2b25e65a436f5d`, dirty working tree. Exact source hashes, wrapper and dependency versions are in the [machine record](records/post-report-product-integration-001-contract-001.json).
- Reproduction: from repository root, `PYTHONPATH=$PWD uv run python reports/generated/post-report-product-integration-001-contract-001/reproduce.py`.
- Artifacts: `reports/generated/post-report-product-integration-001-contract-001/`, including original manifest, 143 check results, injected provider ledger and source archive. Original synthetic backup/restore runtime location is retained in the machine record.
- Predecessor: existing mixed-source recovery harness. This does not replace its historical results.

## Decision context

The [prospective product-completion plan](../04_experiments/post-report-product-completion-001.md) predicts that one explicit new composition can preserve course/source authority and recovery across a complete professor/student workflow. The candidate combines V18 generation with assessed-decay input, analytic-only proactive planning, source-bound model assessment, goal recovery and conversation retrieval. Model responses and final-audit verdicts are injected contracts: they exercise real application boundaries without claiming model quality.

The existing historical composition remains the control/fallback. This run is an integration gate for the candidate, not a randomized or powered superiority comparison. Hard gates are exact source/course authorization, small-cohort suppression, persisted review, withdrawal preventing provider access, and clean restore retaining the correct scoped content. Every declared check must pass.

## Data and sample size

One exposed synthetic journey uses two courses, seven student accounts, a professor and an administrator. Course A has six active learners; five produce the relevant gap signals. PDF, plain-text and Markdown sources are synthetic and approved for local processing. An identically named concept in the other course has a deliberately different value to expose cross-course leakage.

The 143 checks are dependent workflow assertions, not 143 independent learners or quality samples. No confidence interval or population-level effect is claimed. No held-out data, actual student data or external-model call is used.

## Exact configuration

The machine record stores every factory flag and the temporary wrapper source hash. The wrapper uses the real `create_app`, persistent repository, credential/Origin handlers, queued ingestion, profile/release endpoints, tutoring service, cohort aggregation, review, withdrawal and backup/restore. Typed drafts and V2 quality-audit decisions are injected. They report the configured Luna/Sol identities for protocol checking; no actual model capability is measured. The original harness manifest is preserved unchanged; its older generic candidate-override description is supplemented by the explicit wrapper configuration in the machine record.

## Aggregate and slice results

| Measure | Result | Interpretation |
| --- | --- | --- |
| Workflow checks | 143/143 passed | Single synthetic contract journey |
| Student accounts | 7 | Six in primary course, one sentinel |
| Cohort visibility | Suppressed below five; visible at five gap learners | Threshold retained |
| Professor review | Saved, reread and restored | Does not establish reviewer agreement or pedagogical value |
| Sources | PDF, text and Markdown restored | Source bytes and scoped citations checked |
| Withdrawn course | New messages rejected before provider call | Still rejected after clean restore |
| Other course | Its own differing value and citation retained | Cross-course source separation |
| Source tree | Start/end hashes agree | No source mutation during the probe |

## Operational results

The instrument measured 1.999982 seconds for its fixed in-process journey and peak process RSS 336,232,448 bytes. There were 19 injected calls, zero external calls and US$0 external cost. Token counts, provider latency and p95 HTTP latency are not meaningful or measured in this fixture. HTTP/auth/Origin behavior is exercised using TestClient; this is not real network/TLS, sustained load or autonomous worker uptime measurement.

## Failures and surprises

The first launch omitted the repository from `PYTHONPATH` and failed at import before any runtime or model call. The corrected explicit-`PYTHONPATH` invocation completed. No workflow check failed. The difference between the old runner envelope and the temporary injected wrapper is explicitly preserved; it must not be mistaken for an unchanged historical run.

## Validity review and decision

**Keep the integration contract and candidate availability. Do not promote the candidate on this evidence.** The source-binding/audit fixtures provide protocol checks, not independent semantic judgments. No release profile changed. Retain the selected historical profile and explicit prior candidates as fallbacks.

Remaining checks include real model usefulness and failure rates, assessment precision/coverage, API/autonomous-worker lifecycle under the same configuration, 30-day policy comparisons, sustained/failure load and human independent review. This result advances P02/P03/P04 and O03 integration evidence; it does not finish all those items or establish educational effectiveness.
