# Governed full-autonomy V2.1 actual-product confirmation 017

## Outcome

Confirmation 017 is `invalid-execution`. It persisted the two frozen public
canary responses, made zero provider calls, spent USD 0, and stopped before
bulk execution. Hidden gold remained unopened. This attempt therefore provides
no provider-backed product-quality or release evidence.

## Root cause

The replacement T1-v2 autonomous canary also followed a deterministic path.
Its four student turns completed safely, but no learner goal or due opportunity
was created, so the case made no Luna call. The canary assertion expected Luna
and correctly stopped the run.

The postmortem closed three findings:

- `SE7-5`: the test proved only that a canary ID and expected model were
  configured; it did not prove that the exact product path invoked the model.
- `SE7-6`: deterministic perception did not recognize ordinary uncertainty,
  hint-request, and explanation language. That prevented learner-goal and
  follow-up creation when the new grounding fast path skipped semantic planning.
  The network-free provider-failure stand-in also raised directly instead of
  reproducing the live planner's deterministic semantic fallback.
- `SE7-7`: V2 perception and observation records used wall-clock defaults
  instead of the graph input's injected virtual timestamp.

These are harness and product-integration defects, not evidence that the OpenAI
provider was unavailable. No network request was attempted.

## Corrective boundary

The same-package correction allowance is exhausted. Attempt 017 is not resumed
and no third execution of the 016 package is permitted. Authority is revoked.

The product correction expands deterministic perception using general intent
language, preserves the live deterministic semantic fallback in network-free
simulation, and binds V2 observations to the injected clock. A separate
network-free known-set regression is required to verify the correction, but it
cannot replace a fresh provider-backed confirmation.

That regression passed 820/820 at clean revision `49952b1`; it is registered
separately as corrective evidence and makes no release claim.

Any future evaluation must use distinct canaries:

1. a direct provider transport/identity canary; and
2. a product-path canary whose provider invocation was established by an
   executable dry-run route contract, not by its case label.

## Limitations

- Only two public canary responses were persisted.
- Hidden gold remained unopened.
- No provider-backed V3 autonomy quality was measured.
- Real professor fidelity, real student usability, and learning improvement are
  not established.
