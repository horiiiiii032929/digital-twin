# Synthetic professor-profile C0–C2 diagnostic 002

The 12-case C0–C2 diagnostic completed all 36 exact GPT-5.4 mini calls with zero retries for USD 0.03447. Deterministic source truth and policy checks remained authoritative.

The profile had a measurable effect: every C2 response differed from its paired C1 response, and no condition produced an unsupported answer release. That did not translate into acceptable teaching behavior. C2 reached 66.7% action accuracy, 33.3% boundary accuracy, 38.3% answerable fact recall, and 91.7% profile-contract accuracy, below every corresponding frozen gate except lineage and unsupported-release safety.

## Decision

`Refine`. Select no professor-profile method and do not run C3 yet. The result is a synthetic method diagnostic only; the profile is draft and unapproved, so it is not professor-fidelity evidence.

The actual professor must still approve a profile and its calibration packet before the repository can evaluate or claim professor-specific fidelity.
