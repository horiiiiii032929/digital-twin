# Local R1 governed V2.1 release qualification 006

## Decision

`completed-keep` for the local research demo. The exact candidate-v3,
dominance-gate, Luna H+E1 composition remains operational after the
multi-concept assessment-scope correction. T0 remains the immediate rollback.

This does not reverse the academic grounding `No Release` boundary: the
selected evidence gate remains below the absolute factual-quality threshold.

## Results

| Check | Result |
| --- | ---: |
| New internal-CA HTTPS journey | 25/25 |
| Restart persistence | 6/6 |
| Clean backup restore | 6/6 |
| T0 rollback | 3/3 |
| Governed V2.1 restoration | 3/3 |
| Machine-verifiable total | 43/43 |
| Critical browser defects | 0 |
| Provider calls / cost | 0 / USD 0 |

The browser smoke rendered the actual HTTPS build on desktop and the identical
frontend at an exact 390-by-844 viewport. The narrow layout fit without
horizontal overflow. Email, Password, and Sign in had accessible names and a
correct keyboard focus order. The Impeccable mechanical detector returned no
finding for the login surface.

## Invalid predecessor

Attempt 001 is preserved separately. It failed before the journey because an
explanatory note edit had changed the immutable profile hash. Restoring the
exact profile bytes was the only correction; no product method, data, check, or
threshold changed. A second setup preflight established that the OpenAI key in
the root `.env` must be passed into Compose without copying or printing it.

## Limitations

- The evidence gate is operationally stable but below the absolute academic
  factual-quality threshold.
- The journey used deterministic fast paths and made no Luna call.
- The known 10,000+1,000 package was not read, rerun, or rescored.
- True-visual successor 003 is built but not executed.
- The professor-fidelity harness supports only synthetic LLM-proxy evidence;
  it cannot prove fidelity to the real professor without an approved reference.
- This does not establish real-student usability, learning improvement,
  durable hosting, or broad accessibility certification.
