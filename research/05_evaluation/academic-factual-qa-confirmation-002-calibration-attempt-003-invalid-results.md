# Academic factual-QA confirmation 002 calibration — attempt 003

Result ID: `academic-factual-qa-confirmation-002-calibration-attempt-003-invalid`

Decision: **Invalid execution / Refine reviewer transport**

The clean, frozen run started from revision `7f445af`. It reused the unchanged
40 controls, sealed 200-case packet, deterministic truth, and immutable 40/40
Codex votes. The Gemini 3.7 Flash canary completed through the exact Google AI
Studio route: four votes parsed, the returned model/provider identity matched,
and OpenRouter reported 2,656 input tokens, 1,551 output tokens, 5.193 seconds,
and USD 0.00780825.

The direct DeepSeek V4 Pro canary then returned empty response content. The
zero-retry executor recorded the failure, stopped after two calls, made no bulk
calls, and did not open the 200 confirmation cases. DeepSeek supplied no usable
response, token usage, or authoritative cost on this path, so USD 0.00780825 is
the known Gemini cost rather than complete run cost.

This is an operationally invalid execution, not a Gemini-quality,
DeepSeek-quality, factual-QA, or product result. Four Gemini canary votes are
too small and were not interpreted as calibration evidence. No private course
or student data was read or uploaded.

The ignored exclusive ledger is
`reports/generated/academic-factual-qa-confirmation-002-calibration-attempt-003-ledger.json`
with SHA-256
`d3aac5b3f3df8874890897045be1722e9aa2829dd4bce2457b4d0b82d9f9d013`.
Authorization is revoked. A successor must make one explicit reviewer-transport
decision before any new calibration; the sealed 200-case panel remains closed.
