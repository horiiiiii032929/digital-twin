# Conditional revision controls preflight

Before V14 outputs, the packet-author assistant prepared16 fresh scenarios,
each with an adequate and defective typed draft. The root coding assistant
independently read all32 questions, histories, profiles, evidence, drafts and
gold; it accepted16 adequate and16 defective labels. This is second-assistant
review, not human or professor review.

The fresh pairs cover own records, third-party privacy, ordinary privacy terms,
correct and incorrect attempts, new concepts, mixed initial/missing requests,
genuinely ambiguous and uniquely named requests, explicitly resolved history,
source ownership, logical direction and exceptions. Dahlia's adequate draft
isolates the missing date while withholding the decisive new rule. Jade's
one-way implication and explicit equivalence must remain distinct. Haze's
“when fewer than seven” must not be silently strengthened to “only when”; Boreal's
“only after” establishes a necessary condition, not an equivalence.

All112 combined public inputs passed actual product profile validation, typed
proposal validation and shared rendering preflight without provider calls. This
establishes fixture compatibility, not semantic correctness of future output.
The80 previously exposed controls, labels and original packet bytes are unchanged;
V12 and V13 failures remain preserved. The new32 controls are16 paired scenarios,
not32 independent student or source populations. The sealed confirmation was
not accessed.

The ignored directory `reports/generated/conditional-revision-controls-v1/`
contains `supplement.json`, `combined-packet.json`, pre-review byte snapshots and
`preflight-seal.json` binding original/final hashes. Only review metadata and its
linked component hash changed after second review; every context and gold label
is identical. Ordering uses seeds8341 and8342. The
[prospective V14 plan](../04_experiments/2026-09-06-conditional-revision-plan.md)
fixes subgroup gates and requires final rendered-content review separately from
model keep/repair labels and proposed fault/move fields.
