> Historical Codex-assisted disclosure review, retained for evidence navigation.
> “Independently” below describes a separate assistant review, not independent human validation.

# Exposed112 payload disclosure review

Independently read all112 proposals and all56 shared question/source/profile/history contexts. Selected model input consists of authored fiction and previous model responses to that fiction. No real-course passages, identifiable student/instructor records or embedded secret values were observed. Toy names, booking codes, balances and privacy requests are synthetic test premises; no actual PIN or private appointment record is present.

Packet SHA256: `7386fe29e2cfa42e95b49d71d127347d42c994f892fe13fa7df09ecbabae26d6`. All five recorded baseline file digests match. The JSON companion records every case and public-input digest. Email, URL, credential-value and actual-user-identity scans return zero; manual review, rather than these scans alone, supports the finding.

Actual serialization sends question, fictional history, source labels/text, typed proposal, rendered entries and content digests. C2 additionally sends the authored teaching profile; repair receives the same context and machine issue codes, then its generated replacement is re-audited. Gold, provenance paths, real user/professor conversation and unrelated local files are excluded.

The runner creates OpenAiResponsesClient directly. Its hardcoded destination is `https://api.openai.com/v1/responses`; chat sends HTTPS POST with system/user text, model, output cap3000, reasoning effort, strict schema, task metadata and store=false. The environment API credential is used only in Authorization to that API, not in model input. No credential value was inspected. No provider was called during this review.

This is local evidence gathering for reconsideration of the blocked launch, not an override of automatic approval. The user has requested actual LLM evaluations and continued work with budget secondary; the parent must present the concrete payload and destination evidence to review. Disclosure safety is distinct from response quality and from provider-wide retention guarantees.
