# Real-course continuation diagnostic: preflight review

A separate assistant reviewed all eight curated excerpts and32 question/gold/profile pairs before provider execution. The source text, questions and private build script remain under ignored `reports/generated/real-course-continuation-development-v1/`; none is reproduced here. This review is implementation-aware assistant review, not independent human labels or approval of a representative benchmark.

Eight document hashes, page/start/end selections, whitespace-normalized texts and excerpt hashes were independently reproduced from the actual local PDFs and matched the private lineage/packet. All64 supplied gold spans match source substrings. No contact information, student records or assessment answers appeared in the selected passages. This check verifies the selected excerpts; it is not a full-PDF exclusion audit or ingestion-quality test.

The32 authored meanings are grounded in the selected material and explicit synthetic applications. Computer-representation bit-pattern interpretation and unit conversion are consistent; database grouping/derived-attribute examples match the excerpts; stream questions distinguish demand-driven computation from unbounded arrival; cryptography questions preserve block-versus-key-size distinctions. Two-turn cases contain actual visible attempts and require attempt-specific responses. Initial profiles require elicitation; explanatory profiles permit direct answers. Missing historical/performance values and the private-credential request are absent from the source pool.

Two setup refinements were recommended to the author before calls: remove the implication that derived attributes necessarily cannot be stored, and scope the single-use stream abstraction to the supplied lecture rather than every possible streaming API. These are question/label clarity refinements, not threshold changes. Any accepted correction must be reflected in the private rebuild script and packet and hashed before execution. The spans attached to each gold row constitute an available source pool; requiring every span in every answer would be an invalid future scorer interpretation.

The planned26/32 overall,6/8 per course, zero critical and nonnegative paired-gain criteria are appropriately labelled narrow diagnostic gates. Four courses with only two curated passages each do not meet the separate representative400-question confirmation requirement. Review every delivered target and prior turn; retain omitted details, unsupported generalizations and initial disclosure even if citation identity validates. Source permission and provider-transfer configuration must remain explicit in the execution manifest. The existing sealed confirmation remains unopened.

## Final pre-output setup verification

The author applied both recommended question/label clarifications in the private builder and regenerated the packet before calls. The derived-attribute question now asks how values are obtained and explicitly avoids a storage prohibition; three stream questions are scoped to the supplied lecture. All32 gold rows explicitly identify their spans as an available source pool, not universally required answer content. All32 profiles construct through the actual product model. The reviewed packet SHA-256 is `cfa7eacc2fe6fd2ae91bf83840c99937c5f7e1ddf97faf605d42facec3cac406`. No outcome-dependent label change or threshold change occurred.
