# Deterministic-primary development checkpoint 004 build result

## Outcome

`Go Deeper` to one separately authorized 500-case candidate plus 100-case
any-hit control run. Reviewer calibration is no longer a prerequisite and no
new evaluator search is planned.

This is build and network-free simulation evidence. It is not a product-quality
result and does not authorize the untouched 10,000-case evaluation.

## Method

- Deterministic source-linked truth and deterministic action, claim, citation,
  boundary, and policy scoring are authoritative.
- `gpt-5.4-mini-2026-03-17` performs public question wording and T0 answer
  generation through the direct OpenAI Responses API.
- `gpt-5.4-2026-03-05` reviews wording and audits every deterministic product
  failure plus a seeded 40-case passing sample after scoring.
- GPT-5.4 cannot change deterministic metrics. A malformed or missing audit
  response becomes a limitation; only a possible authoritative source-truth
  defect requires researcher review.
- Both exact models use strict structured outputs, `store: false`, no router,
  no fallback, and zero retries. They are separate model configurations from
  one provider family, not independent providers.

The bounded checkpoint permits at most 50 wording calls, 500 candidate calls,
100 control calls, and 54 advisory calls. The aggregate emergency ceiling is
704 calls and USD 23. All execution authority remains false in this build.

## Evidence

- Five network-free terminal simulations cover Keep, wording failure, product
  failure, malformed advisory review, and a possible truth defect.
- A malformed advisory review leaves the simulated deterministic Keep result
  unchanged and is counted as a limitation.
- The product response executor contains no hidden-gold or scoring dependency.
  Gold can open only after both durable response ledgers complete.
- Runtime packages retain 500 candidate and 100 paired control identities with
  exact source-linked gold hashes and no final-split access.
- The OpenAI structured-output payload is strict, non-stored, and bound to the
  exact GPT-5.4 snapshots documented by official OpenAI documentation on
  2026-08-28.
- Focused checkpoint and freeze tests pass with zero provider calls.

## Limitations and next action

- No provider call, token, paid cost, private-data read, product-quality
  measurement, or final-split access occurred.
- Same-provider advisory review is disclosed and is not treated as independent
  ground truth.
- Question wording remains a prospective quality stage; a valid wording or
  product failure stops scaling instead of beginning another prompt-tuning
  loop.
- A development pass prepares a separately frozen 10,000-case run; it does not
  authorize it.

The next action is to publish this build, refresh model and pricing metadata,
run a clean no-call preflight, and request explicit authorization for
`academic-factual-qa-open-10000-development-checkpoint-004`.
