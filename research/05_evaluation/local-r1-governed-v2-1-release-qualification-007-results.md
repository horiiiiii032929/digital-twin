# Local R1 governed V2.1 release qualification 007

## Decision

`completed-keep` for the local R1.1 research demo. The final governed V2.1
composition remains operational after the professor-feedback workflow was
completed. The release retains `text-ocr-fallback`, and T0 remains the
immediate rollback.

This operational result does not reverse the academic grounding limit, select
the failed Jina visual product candidate, or establish professor fidelity.

## Results

| Check | Result |
| --- | ---: |
| Internal-CA HTTPS journey | 25/25 |
| Restart persistence | 6/6 |
| Clean backup restore | 6/6 |
| T0 rollback | 3/3 |
| Governed V2.1 restoration | 3/3 |
| Machine-verifiable total | 43/43 |
| Critical browser defects | 0 |
| Live API p95 | 5.03 ms |
| Provider calls / cost | 0 / USD 0 |

The actual HTTPS interface passed desktop and exact 390-by-844 Playwright
smoke checks without horizontal overflow. Email, Password, and Sign in had
accessible names and the correct keyboard order. One expected unauthenticated
`401` session request was observed; there were no browser warnings or critical
UI defects.

The backup used schema version 16 and contained seven checksum-verified data
files. The clean restore reproduced the six persisted-state checks, and the
primary stack returned successfully from T0 rollback to governed V2.1.

## Invalid predecessor

Attempt 001 is preserved separately. The API correctly failed closed because
the first Compose launch did not forward the separately stored root OpenAI
credential. The correction passed the credential in the process environment
without printing or copying it. No product method or qualification check
changed.

## Limitations

- The selected grounding gate remains the best measured development floor,
  not an absolute factual-quality pass.
- The visual product comparison validly Refined to text/OCR fallback.
- The C0-C3 proxy was invalid and produced no professor-fidelity result.
- This journey used deterministic fast paths and made no Luna call; immutable
  confirmation 024 remains the provider-backed autonomy evidence.
- The known 10,000+1,000 package was not read, rerun, or rescored.
- This result does not establish real-professor fidelity, real-student
  usability, learning improvement, durable hosting, or broad accessibility.
