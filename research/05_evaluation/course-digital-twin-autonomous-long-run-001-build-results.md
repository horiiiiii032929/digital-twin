# Evaluation result: autonomous long-run 001 build

## Decision question

Can the remaining #153 grounding decision, conditional #157 provider-backed
autonomy evaluation, local release regression, and evidence publication run
under one finite authority without repeated approval pauses or reuse of the
sealed Program 011 final set?

## Frozen method

- Program: `course-digital-twin-autonomous-long-run-001`.
- Global emergency ceiling: USD 200; this is a stop, not a target.
- Stage 1: 500-case grounding candidate plus fixed 100-case control.
- Stage 2: provider-backed 820-case actual-product autonomy evaluation, opened
  only by a Stage-1 `completed-keep` result.
- Stage 3: network-free local release, persistence, and rollback regression.
- Stage 4: evidence synthesis and professor-ready publication.
- Same-case quality reruns are forbidden. A valid Refine result stops only its
  dependent branch; orthogonal regression and publication continue.

Program 011's 10,000 candidate plus 1,000 control result remains immutable
known-benchmark evidence. It is referenced in the final publication and is not
rerun or converted into a new confirmatory claim.

## Build result

Validation, the network-free transition simulation, focused #153/#157 tests,
and repository execution-freeze coverage passed with zero provider calls. The
SQLite program ledger binds the program manifest, code revision, stage status,
cost, and resume state. Exact child instruments remain independently bounded by
the repository freeze.

Outcome: **Go Deeper**. Execute the already authorized finite program from a
clean revision. No additional stage authorization is required.

Attempt 001 later stopped before provider I/O because the direct OpenAI
transport declaration omitted its first-party flag and local regression named
two moved tests. The invalid result is preserved separately. Attempt 002 is the
single permitted harness-only correction and changes neither the evaluation
method nor its evidence.

## Limitations

- No product-quality measurement occurred in this build checkpoint.
- Real professor fidelity, real-student usability, and learning improvement
  remain outside LLM-only evidence.
- The mutable `gpt-5.6-terra` identity must remain exact during the live run.
- Security, privacy, gold isolation, ledger integrity, model identity, loop,
  and global-cost hard stops remain active.
