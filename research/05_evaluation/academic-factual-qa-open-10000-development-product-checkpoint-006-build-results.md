# Immutable-index product checkpoint 006 build result

## Outcome

`Go Deeper` to one separately authorized 500-case T0 candidate plus 100-case
paired any-hit control run. Checkpoint 006 removes the demonstrated retrieval
startup blocker without changing the known development questions, hidden gold,
product conditions, deterministic gates, or decision policy.

This is network-free build evidence. It does not measure product quality and
does not authorize the sealed 10,000-case run.

## Frozen successor

- Both conditions verify and load the exact four immutable artifacts selected
  by `retrieval-index-lifecycle-development-001` / AFQC-068.
- The runtime may embed only queries; document embedding and index construction
  are prohibited in the product path.
- Candidate and control use the same corpus, immutable indexes, exact GPT-5.4
  mini generator, policy, and public inputs. Only the evidence gate differs.
- The 452 accepted model questions and 48 explicit canonical fallbacks remain
  unchanged. No wording provider call is reopened.
- Hidden gold opens only after all 500 candidate and 100 control responses are
  durable. Deterministic source, action, claim, citation, and policy scoring
  remains authoritative.
- Routine GPT-5.4 nano review and at most 12 full GPT-5.4 truth escalations are
  advisory. The run remains bounded to 666 calls, zero retries, and USD 8.

## Verification

- Thirty-three focused checkpoint, historical-preservation, and freeze tests
  pass.
- Five finite network-free outcomes pass: Keep, product Refine, provider
  invalid, advisory limitation, and unresolved truth review.
- The no-call preflight verifies the exact four local index artifacts and fails
  closed when any artifact is unavailable or changed.
- Current official OpenAI documentation still lists the three exact dated
  snapshots, Responses API support, structured outputs, and the recorded token
  prices on 2026-08-28.
- No provider call, token use, private-data read, hidden-gold opening, final-set
  access, or paid cost occurred.

## Decision and next action

The successor is finite and ready for repository-wide verification. After that
gate and publication, the only next input is explicit one-time paid execution
authorization for
`academic-factual-qa-open-10000-development-product-checkpoint-006`.

A valid quality failure will be registered and stop scaling. A pass may prepare
the sealed 10,000-case checkpoint, which still requires a separate build,
preflight, and authorization.
