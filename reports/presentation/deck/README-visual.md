# Revised presentation

Use `digital-twin-presentation-improved.pptx` and the matching PDF. Earlier versions are retained.

This revision replaces seven result tables with ten editable charts, enlarges the native draw.io diagrams, shortens the opening, and places the experimental comparison and interpretation in the slide body. It explains project-specific quantities where they are used. The main talk retains the failed factual designs, rejecting verifier, delivery proxy, completion defect, wording failure, and evaluation corrections. BKT arithmetic is now backup.

There are 32 main slides, Questions on slide 33, and nine backup slides. English speaker notes contain approximately 5,039 narration words, excluding diagram-source metadata. At 170–180 words per minute, narration plus the 4:06 video takes approximately 32.1–33.7 minutes before transitions. Rehearse to set the final pace.

The actual-software video is embedded on slide 4 for click-to-play presentation. The PDF shows a static poster. The separate MP4 is a playback fallback. The new demo uses synthetic accounts, deterministic services and virtual time, and remains distinct from the historical model-backed studies. Native Microsoft PowerPoint playback has not been tested here.

The PPTX includes native editable text, tables and charts with embedded workbook snapshots of the cited literal data. The snapshots do not reproduce evaluation source formulas. All diagrams retain editable draw.io files, including the full original versions and the larger presentation exports. The submitted report is unchanged.

The slide renders were individually reviewed. The exported PDF was additionally checked for chart labels and the dense backup table. Package, layout, fonts, editable-chart data and Artifact Tool import checks passed. Sources remain in each slide's notes and the standalone script.
