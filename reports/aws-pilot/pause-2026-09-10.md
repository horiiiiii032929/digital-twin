# Pilot paused for local testing

On 10 September 2026 Singapore time the user requested pausing AWS to save cost
while coding fixes are tested locally. The EC2 instance
`i-0fde1692c45c87640` was stopped using profile `digital-twin` in `ap-southeast-1`;
its state was read back as `stopped`. The recurring start schedule
`DigitalTwinPilot-OfficeHoursStart70999534-1R6IP3H9TSFCB` was set to `DISABLED`,
preserving all other schedule fields. Existing stop schedules are harmless and
remain unchanged. Do not restart the pilot or re-enable automatic starts without
a new user instruction. Check manual schedule drift before any future CDK deployment.

Restore information is retained locally under `output/aws-pilot-pause/`:
original and paused start schedule, EC2 stop response, NAT gateway and dependent
route-table configuration. Storage, snapshots, addresses and other retained
resources can still incur charges while compute is stopped.

The NAT gateway `nat-01af5cc7918c1ca64` has no pause operation. It is still present
pending the user's choice about temporary deletion. Removing it would require
recreation and route repair before the next AWS run; the Elastic IP would be retained.
No network resources or data have been deleted as part of this pause.

The pending `slide-codefix-002` source changes have not been deployed. Local tests
use synthetic provider transports; they do not require the AWS host or real model
calls and do not establish live model quality.
